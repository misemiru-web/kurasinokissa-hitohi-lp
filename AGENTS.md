# AGENTS.md

## 目的

このファイルは、ミセミルWebが管理するWeb制作リポジトリでCodexが守る共通実装ルールです。
案件固有の事実、技術構成、更新方法、公開情報は `README.md` を参照してください。

## 1. 指示の優先順位

1. ユーザーが現在のタスクで明示した指示
2. リポジトリ直下の `AGENTS.md` にある案件固有ルール
3. `README.md`、要件定義書、QAチェックリスト等に記録された確認済み情報
4. この共通ルール

内容が競合する場合や正式情報を確認できない場合は、推測で進めず `要確認` として報告する。

## 2. 作業前の確認

リポジトリ内に存在するものを次の順で確認する。

1. `AGENTS.md`
2. `CLAUDE.md`
3. `README.md`
4. `package.json`、lockfile、設定ファイル
5. 既存ソースコードとディレクトリ構成
6. 要件定義書、正式情報、素材資料
7. QAチェックリスト、制作上の指示書
8. `git status` と現在のbranch

存在しないファイル、script、外部サービスを前提にしない。

## 3. 最重要ルール

- 店舗情報、契約状況、支払状況、承認状況、公開状況、保守範囲を推測しない。
- 店名、住所、営業時間、定休日、電話番号、料金、メニュー、サービス内容、予約方法、SNS URL等は、確認済み資料またはユーザーの明示指示を根拠にする。
- 仮情報、ダミーリンク、`TODO`、サンプル用表記を正式情報として扱わない。
- 依頼されていない大幅なデザイン変更、機能追加、ページ追加、外部サービス追加を行わない。
- 既存の正常な機能、デザイン、SEO設定を不必要に壊さない。
- パスワード、API秘密鍵、秘密トークン、決済秘密情報、不要な個人情報をコード、文書、Git履歴へ保存しない。

## 4. 技術方針

- 既存案件では現在の技術構成を最優先し、理由なく別のフレームワークへ移行しない。
- HTML / CSS / JavaScriptだけのサイトへ、依頼なくpackage managerやbuild工程を追加しない。
- 新規の正式制作案件ではNext.js / Reactを候補にできるが、要件、運用、ホスティング条件を確認して決定する。
- 不要な依存関係やライブラリを追加しない。
- 既存のディレクトリ構成、命名規則、コンポーネント設計を尊重する。
- package managerはlockfile等から判定し、勝手に変更しない。
- 生成物や外部ツール由来のbundleは、編集元と再生成方法を確認せず直接変更しない。

## 5. UI・アクセシビリティ

- PC、スマートフォン、代表的なタブレット幅で重大な崩れがないようにする。
- 文字切れ、要素重なり、不自然な横スクロール、主要CTAの欠落を避ける。
- 既存デザインの意図を維持し、依頼なしに全面的なリデザインをしない。
- 意味のある画像には適切な `alt` を設定し、装飾画像では不要な代替テキストを付けない。
- キーボード操作、フォーカス表示、色のコントラスト等を既存実装の範囲で悪化させない。

## 6. コンテンツ・素材・権利

- 店舗提供素材など、利用根拠を確認できる素材を優先する。
- 権利不明の画像、Googleマップの写真、第三者SNS投稿を新たに取得・転載しない。
- AI生成画像を公開対象へ追加するときは、使用方針と了承状況を確認する。
- 画像差し替え時は表示崩れ、容量、ファイルパスも確認する。
- 顧客情報や制作資料を、必要性のない公開ディレクトリへ置かない。

## 7. SEO基本設定

案件に存在する次の設定を不必要に壊さず、変更時は相互の整合性を確認する。

- `title`、`meta description`
- H1 / H2等の見出し構造
- canonical、robots、sitemap
- LocalBusiness等の構造化データ
- favicon、OGP

URL、店舗情報、公開状態を推測して設定しない。検索順位、インデックス登録、売上成果を保証する表現を追加しない。

## 8. リンク・フォーム・外部サービス

- 電話、SNS、Googleマップ、予約サービス、CTA等を変更した場合は遷移先を確認する。
- フォーム変更時は既存の送信先、必須項目、エラー処理、個人情報の扱いを壊さない。
- ミセミルWeb側で業務上不要な個人情報を新たに保存する実装を追加しない。
- 外部サービスの追加、削除、設定変更は明示依頼がある場合だけ行う。
- 認証情報の値を画面、ログ、回答、文書へ表示しない。

## 9. Git・変更範囲

- 作業対象外のファイルを変更しない。
- 無関係な一括整形、リファクタリング、ファイル移動を避ける。
- 未コミット変更を勝手に破棄、上書き、stashしない。
- 既存Git履歴を破壊しない。
- force push、reset、rebase、重要branch削除、remote変更等は、ユーザーの明示依頼なしに行わない。
- commitやpushの可否と対象範囲は、そのタスクの指示に従う。
- 公開済み安定版を示すcommitやtagが確認できる場合は尊重する。

## 10. Build・検証

作業完了前に、案件で利用可能な範囲だけを検証する。

1. package managerとlockfileを確認する。
2. `package.json` がある場合はscriptsを確認する。
3. build、lint、typecheck、test等の既存scriptが今回の変更に関係する場合は実行する。
4. 静的サイトでは専用コマンドを創作せず、差分、HTML、リンク、主要表示を確認する。
5. エラーは今回の変更によるものか既存問題かを区別する。
6. 実行していない検証を実行済みと報告しない。

## 11. 公開・デプロイ

- 本番公開、本番環境変更、GitHubへのpushは、ユーザーから明示的に依頼された場合のみ行う。
- ホスティング、ドメイン、DNS、公開URL、公開先の所有関係を推測しない。
- 既存のデプロイ設定を、内容と影響を確認せず変更しない。
- プレビューと本番公開を区別する。
- READMEや確認済み資料に承認・支払等の公開条件が明記されている場合は、その条件を満たした事実を確認できるまで公開しない。
- 外部サービスの料金や利用条件を判断するときは、実行時点の公式情報を確認する。

## 12. README

実装、技術、公開方法、外部サービス、更新方法が変わる場合は `README.md` の更新要否も確認する。

特に次が変わったときはREADMEを見直す。

- フレームワーク、主要ライブラリ、必要バージョン
- セットアップ、開発起動、build方法
- ホスティング、デプロイ、ドメイン、DNS
- 外部サービス、フォーム、解析、SEO
- 店舗情報や画像を更新する主な場所
- 公開済み安定版、復元方法、引継ぎ上の注意

確認できない項目は `要確認` と記載し、認証情報そのものは書かない。

## 13. 作業完了時の報告

- 変更内容と主な変更ファイル
- 実行したbuild / test / lint等と結果
- 実行していない検証
- 未確認事項、残課題、関係者確認が必要な内容
- commit、push、公開の実施有無

確認できていない項目を確認済みとして扱わない。

## 14. このファイルの位置づけ

この `AGENTS.md` はCodex向けの共通実装ルールであり、制作SOP、契約、公開前QA、READMEを置き換えるものではない。
各リポジトリでは末尾の `案件固有ルール` と、ユーザーの最新指示を優先する。

---

## 案件固有ルール｜暮らしの喫茶 ひとひ

- 本案件は営業提案用サンプルLP。
- `docs/requirements/` の要件定義書を案件情報の基準とする。
- `docs/design/` の完成イメージをUI実装の視覚基準とする。
- 全体モックは全体構成・色・余白・セクション接続の基準とする。
- 個別セクションモックは、そのセクションのレイアウト・画像配置・文字配置について全体モックより優先する。
- デザイン画像と要件定義書が矛盾する場合は要件定義書を優先する。
- Hero画像はユーザーが指定済みの素材を使用し、独自に変更しない。
- `public/images/` 内の画像を優先して使用し、新たなネット画像を取得しない。
- 固定定休日、電話番号、駐車場台数、席数、現行価格、予約要否を推測で追加しない。
- 主CTAはInstagramで最新の営業日を確認する導線。
- 副CTAはGoogleマップへの導線。
- ページ下部に「営業提案用サンプル」と表示する。
- 初回実装では要件外のセクション・機能・ライブラリを追加しない。
- ユーザー確認前にcommit、push、deployを行わない。

